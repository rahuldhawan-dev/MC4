@model CreateEquipmentPurpose

@using (Form.BeginForm("Create", "EquipmentPurpose", new {Model.Id}))
{
    <div class="field-column fc200">
        @Html.Partial("_Form")
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Search")
                           </text>)
    </div>
}