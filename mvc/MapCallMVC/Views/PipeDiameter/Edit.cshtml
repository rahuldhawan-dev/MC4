@model EditPipeDiameter

@using (Form.BeginForm("Update", "PipeDiameter", new {Model.Id})) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Show", "PipeDiameter", new {Model.Id})
                           </text>)
    </div>
}