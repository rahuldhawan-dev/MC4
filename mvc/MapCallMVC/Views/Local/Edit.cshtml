@model EditLocal

@using (Form.BeginForm("Update", "Local", new {Model.Id})) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)

        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Show", "Local", new {Model.Id})
                           </text>)
    </div>
}