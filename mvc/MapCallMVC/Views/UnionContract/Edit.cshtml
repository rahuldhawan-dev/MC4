@model EditUnionContract

@using (Form.BeginForm("Update", "UnionContract", new {Model.Id})) {
    <div class="field-column fc200">
        @Html.DisplayFor(m => m.Id)
        
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Show", "UnionContract", new {Model.Id})
                           </text>)
   </div>
}