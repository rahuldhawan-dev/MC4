@model MapCallMVC.Areas.Environmental.Models.ViewModels.EditChemicalInventoryTransaction

@using (Form.BeginForm("Update", "ChemicalInventoryTransaction", "Environmental", new {Model.Id})) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Show", "ChemicalInventoryTransaction", new {Model.Id})
                           </text>)
    </div>
}