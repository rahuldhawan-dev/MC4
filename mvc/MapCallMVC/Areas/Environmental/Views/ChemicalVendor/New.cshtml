@model MapCallMVC.Areas.Environmental.Models.ViewModels.CreateChemicalVendor

@using (Form.BeginForm("Create", "ChemicalVendor", "Environmental")) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Index", "ChemicalVendor")
                           </text>)
    </div>
}