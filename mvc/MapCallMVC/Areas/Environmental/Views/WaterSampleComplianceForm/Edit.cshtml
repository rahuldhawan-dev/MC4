@model MapCallMVC.Areas.Environmental.Models.ViewModels.EditWaterSampleComplianceForm

@Html.ScriptFor("~/Scripts/Areas/Environmental/WaterSampleComplianceForm/Form.js", ScriptLoadType.LoadFromPartial)

@using (Form.BeginForm("Update", "WaterSampleComplianceForm", "Environmental", new { Model.Id }))
{
    @Html.Partial("_FormFields", Model)

    @Html.ButtonGroup(@<text>
        @Control.SubmitButton("Save")
        @Html.LinkButton("Cancel", "Show", "WaterSampleComplianceForm", new { Model.Id })
    </text>)
}