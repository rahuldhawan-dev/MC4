@model MapCallMVC.Areas.Environmental.Models.ViewModels.EditChemicalStorage

@using (Form.BeginForm("Update", "ChemicalStorage", "Environmental", new {Model.Id})) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Show", "ChemicalStorage", new {Model.Id})
                           </text>)
    </div>
}