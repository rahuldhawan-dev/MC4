@model MapCallMVC.Areas.Environmental.Models.ViewModels.CreateChemicalUnitCost

@using (Form.BeginForm("Create", "ChemicalUnitCost", "Environmental")) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Index", "ChemicalUnitCost")
                           </text>)
    </div>
}