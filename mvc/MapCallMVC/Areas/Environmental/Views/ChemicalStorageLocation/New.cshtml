@model MapCallMVC.Areas.Environmental.Models.ViewModels.CreateChemicalStorageLocation

@using (Form.BeginForm("Create", "ChemicalStorageLocation", "Environmental")) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Index", "ChemicalStorageLocation")
                           </text>)
    </div>
}