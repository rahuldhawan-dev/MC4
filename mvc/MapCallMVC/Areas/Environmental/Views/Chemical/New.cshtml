@model MapCallMVC.Areas.Environmental.Models.ViewModels.CreateChemical

@using (Form.BeginForm("Create", "Chemical", "Environmental")) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Index", "Chemical")
                           </text>)
    </div>
}