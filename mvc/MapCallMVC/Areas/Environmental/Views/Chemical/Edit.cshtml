@model MapCallMVC.Areas.Environmental.Models.ViewModels.EditChemical

@using (Form.BeginForm("Update", "Chemical", "Environmental", new {Model.Id})) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Show", "Chemical", new {Model.Id})
                           </text>)
    </div>
}