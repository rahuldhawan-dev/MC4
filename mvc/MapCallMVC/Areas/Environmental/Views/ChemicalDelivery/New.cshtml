@model MapCallMVC.Areas.Environmental.Models.ViewModels.CreateChemicalDelivery

@using (Form.BeginForm("Create", "ChemicalDelivery", "Environmental")) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Index", "ChemicalDelivery")
                           </text>)
    </div>
}