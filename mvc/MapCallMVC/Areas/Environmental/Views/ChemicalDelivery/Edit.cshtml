@model MapCallMVC.Areas.Environmental.Models.ViewModels.EditChemicalDelivery

@using (Form.BeginForm("Update", "ChemicalDelivery", "Environmental", new {Model.Id})) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Show", "ChemicalDelivery", new {Model.Id})
                           </text>)
    </div>
}