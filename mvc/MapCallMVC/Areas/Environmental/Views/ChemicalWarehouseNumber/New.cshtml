@model MapCallMVC.Areas.Environmental.Models.ViewModels.CreateChemicalWarehouseNumber

@using (Form.BeginForm("Create", "ChemicalWarehouseNumber", "Environmental")) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Index", "ChemicalWarehouseNumber")
                           </text>)
    </div>
}