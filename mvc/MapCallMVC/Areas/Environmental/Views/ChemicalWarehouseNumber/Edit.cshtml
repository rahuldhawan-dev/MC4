@model MapCallMVC.Areas.Environmental.Models.ViewModels.EditChemicalWarehouseNumber

@using (Form.BeginForm("Update", "ChemicalWarehouseNumber", "Environmental", new {Model.Id})) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Show", "ChemicalWarehouseNumber", new {Model.Id})
                           </text>)
    </div>
}