@model MapCallMVC.Areas.Production.Models.ViewModels.EditProductionWorkOrderMeasurementPointValue

@using (var form = Form.BeginForm("Update", "ProductionWorkOrderMeasurementPointValue"))
{
    form.HtmlAttributes.Add("id", "productionWorkOrderMeasurementPointValuesForm");
    
    <input type="hidden" name="Id" id="Id" value="@Model.Id" />
    @* //TODO: Ask Ross about why this assumes the parent Id when used in a partial instead of this Id.
        @Control.HiddenFor(x => Model.Id)*@

    @Control.HiddenFor(x => x.ProductionWorkOrder)
    @Control.HiddenFor(x => x.Equipment)
    @Control.HiddenFor(x => x.MeasurementPointEquipmentType)
    @Html.EditorWithoutWrapperFor(x => x.Value)
                          @Control.SubmitButton("Update Measurement Point")
}