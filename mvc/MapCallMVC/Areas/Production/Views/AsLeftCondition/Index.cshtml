@model IEnumerable<AsLeftCondition>

@(Html.TableFor()
      .WithCaption("Records found: " + Model.Count())
      .TemplateColumnFor(x => Html.AuthorizedActionLink("View", "Show", "AsLeftCondition", new { id = x.Id }))
      .ColumnFor(x => x.Description))    
     