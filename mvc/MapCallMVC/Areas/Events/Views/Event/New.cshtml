@model MapCallMVC.Areas.Events.Models.ViewModels.CreateEvent
@using (Form.BeginForm("Create", "Event", "Events"))
{
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)

        @Html.ButtonGroup(@<text>
                              @Control.SubmitButton("Save")
                              @Html.LinkButton("Cancel", "Index", "Event")
                           </text>)
    </div>
}