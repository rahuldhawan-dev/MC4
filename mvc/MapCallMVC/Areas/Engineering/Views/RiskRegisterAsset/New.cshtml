@model MapCallMVC.Areas.Engineering.Models.ViewModels.RiskRegisterAssets.CreateRiskRegisterAsset

@using (Form.BeginForm("Create", "RiskRegisterAsset", "Engineering")) 
{
    <div class="field-column fc150">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Save")
            @Html.LinkButton("Cancel", "Search")
        </text>)
    </div>
}
