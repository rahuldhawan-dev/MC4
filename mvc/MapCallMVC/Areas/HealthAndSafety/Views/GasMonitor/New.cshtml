@model MapCallMVC.Areas.HealthAndSafety.Models.ViewModels.CreateGasMonitor

@using (Form.BeginForm("Create", "GasMonitor"))
{
    <div class="field-column fc200">
        @Html.Partial("_Form")
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Save")
            @Html.LinkButton("Cancel", "Search")
        </text>)
    </div>
}