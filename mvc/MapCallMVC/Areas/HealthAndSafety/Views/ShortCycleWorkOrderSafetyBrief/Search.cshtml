@model MapCallMVC.Areas.HealthAndSafety.Models.ViewModels.SearchShortCycleWorkOrderSafetyBrief

@using (Form.BeginForm("Index", "ShortCycleWorkOrderSafetyBrief", "HealthAndSafety"))
{
<div class="field-column fc200 searchFields">
    @Html.EditorFor(x => x.State)
    @Html.EditorFor(x => x.OperatingCenter)
    @Html.EditorFor(x => x.FSR)
    @Html.EditorFor(x => x.DateCompleted)
    @Html.ButtonGroup(@<text>
        @Control.SubmitButton("Search").WithId("Search")
        @Control.ResetButton()
    </text>)
</div>
}