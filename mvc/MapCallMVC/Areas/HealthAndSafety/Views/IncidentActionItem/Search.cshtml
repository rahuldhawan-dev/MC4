@model MapCallMVC.Areas.HealthAndSafety.Models.ViewModels.SearchIncidentActionItem
    
@using (Form.BeginForm("Index", "IncidentActionItem"))
{
    <div class="field-column fc200 searchFields">

        @Html.EditorFor(x => x.State)
        @Html.EditorFor(x => x.OperatingCenter)
        @Html.EditorFor(x => x.Completed)
        @Html.EditorFor(x => x.DateCompleted)
        @Html.EditorFor(x => x.Type)

        @Html.ButtonGroup(@<text>
                              @Control.SubmitButton("Search").WithId("Search")
                              @Control.ResetButton()
                           </text>)
    </div>
}
