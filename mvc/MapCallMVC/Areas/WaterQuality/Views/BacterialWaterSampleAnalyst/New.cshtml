@model MapCallMVC.Areas.WaterQuality.Models.ViewModels.CreateBacterialWaterSampleAnalyst

@using (Form.BeginForm("Create", "BacterialWaterSampleAnalyst", "WaterQuality")) {
    <div class="field-column fc200">
	    <div class="field-column fc200">
		    @Html.EditorFor(x => x.OperatingCenter)
		    @Html.EditorFor(x => x.Employee)
		    @Html.EditorFor(x => x.IsActive)
	    </div>
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Index", "BacterialWaterSampleAnalyst")
                           </text>)
    </div>
}