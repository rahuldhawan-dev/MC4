@model MapCallMVC.Areas.FieldOperations.Models.ViewModels.ServiceInstallationMaterialViewModel
    
@using (Form.BeginForm("Update", "ServiceInstallationMaterial", new { id = Model.Id }))
{
    <div class="field-column fc200">
        @Html.Partial("_Form")
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Save")
            @Html.LinkButton("Cancel", "Show", "ServiceInstallationMaterial", new { Model.Id })
        </text>)
    </div>
}