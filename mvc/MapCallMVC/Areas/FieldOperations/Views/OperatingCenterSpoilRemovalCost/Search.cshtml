@model MapCallMVC.Areas.FieldOperations.Models.ViewModels.SearchOperatingCenterSpoilRemovalCost

@using (var f = Form.BeginForm("Index", "OperatingCenterSpoilRemovalCost"))
{
    <div class="field-column fc200">
        @Html.EditorFor(x => x.State)
        @Html.EditorFor(x => x.OperatingCenter)

        @Html.ButtonGroup(@<text>
                              @Control.SubmitButton("Search").WithId("Search")
                              @Control.ResetButton()
                           </text>)
    </div>
}