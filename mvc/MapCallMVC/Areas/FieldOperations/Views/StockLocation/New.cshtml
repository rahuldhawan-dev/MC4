@model MapCallMVC.Areas.FieldOperations.Models.ViewModels.StockLocationViewModel
    
@using (Form.BeginForm("Create", "StockLocation"))
{
    <div class="field-column fc200">
        @Html.Partial("_Form")
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Save")
            @Html.LinkButton("Cancel", "Search")
        </text>)
    </div>
}
