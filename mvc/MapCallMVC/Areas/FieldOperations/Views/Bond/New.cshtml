@model MapCallMVC.Areas.FieldOperations.Models.ViewModels.CreateBond

@using (Form.BeginForm("Create", "Bond", "FieldOperations")) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Index", "Bond")
                           </text>)
    </div>
}