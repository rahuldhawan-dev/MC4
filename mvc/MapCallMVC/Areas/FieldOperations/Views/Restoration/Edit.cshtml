@model MapCallMVC.Areas.FieldOperations.Models.ViewModels.EditRestoration

@using (Form.BeginForm("Update", "Restoration", "FieldOperations", new { Model.Id }))
{
    <div class="field-column fc200">
        @Html.Partial("_Form")
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Save")
            @Html.LinkButton("Cancel", "Show", "Restoration", new { Model.Id })
        </text>)
    </div>
}