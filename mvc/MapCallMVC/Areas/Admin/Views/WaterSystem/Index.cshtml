@model MapCallMVC.Areas.Admin.Models.SearchWaterSystem

@(Html.TableFor(x => Model)
    .WithCaption("Total Records: " + Model.Count)
      .TemplateColumnFor(x => Control.ActionLink("View", "Show", "WaterSystem", new { id = x.Id }, null))
      .ColumnFor(x => x.Description)
      .ColumnFor(x => x.LongDescription)
      .WithPaginatedFooter())

