@model MapCallMVC.Areas.Admin.Models.ViewModels.RoleGroups.SearchRoleGroup

@using (Form.BeginForm("Index", "RoleGroup"))
{
<div class="field-column fc200 searchFields">
    @Html.EditorFor(x => x.Name)

    @Html.ButtonGroup(@<text>
        @Control.SubmitButton("Search")
        @Control.ResetButton()
    </text>)
</div>
}