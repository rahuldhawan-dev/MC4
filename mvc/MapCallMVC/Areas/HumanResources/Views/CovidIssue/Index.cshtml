@model MapCallMVC.Areas.HumanResources.Models.ViewModels.SearchCovidIssue

@{
    ActionBarHelper.AddExportLink();
}

@(Html.TableFor(_ => Model)
      .WithCaption("Records found: " + Model.Count)
      .TemplateColumnFor(x => Html.ActionLink("View", "Show", new { x.Id}))
      .SortableColumnFor(x => x.Employee.OperatingCenter.State, "st.Abbreviation")
      .SortableColumnFor(x => x.Employee.OperatingCenter, "oc.OperatingCenterCode")
      .SortableColumnFor(x => x.Employee, "emp.LastName")
      .SortableColumnFor(x => x.Employee.PhoneCellular, "emp.PhoneCellular")
      .SortableColumnFor(x => x.Employee.ReportsTo, "rpt.LastName")
      .SortableColumnFor(x => x.HumanResourcesManager, "HumanResourcesManager.LastName")
      .SortableColumnFor(x => x.Employee.ReportingFacility, "fac.FacilityName")
      .ColumnFor(x => x.PositionGroup, "PositionGroup")
      .ColumnFor(x => x.PositionGroupDescription, "PositionGroupDescription")
      .ColumnFor(x => x.BusinessUnit)
      .ColumnFor(x => x.BusinesUnitDescription)
      .SortableColumnFor(x => x.SupervisorsCell)
      .SortableColumnFor(x => x.LocalEmployeeRelationsBusinessPartner)
      .SortableColumnFor(x => x.LocalEmployeeRelationsBusinessPartnerCell)
      .SortableColumnFor(x => x.PersonnelArea)
      .SortableColumnFor(x => x.RequestType, "reqt.Description")
      .SortableColumnFor(x => x.SubmissionStatus, "ss.Description")
      .SortableColumnFor(x => x.SubmissionDate)
      .SortableColumnFor(x => x.HealthDepartmentNotification)
      .SortableColumnFor(x => x.OutcomeCategory, "cat.Description")
      .SortableColumnFor(x => x.QuarantineStatus, "qs.Description")
      .SortableColumnFor(x => x.WorkExposure)
      .SortableColumnFor(x => x.AvoidableCloseContact)
      .SortableColumnFor(x => x.FaceCoveringWorn)
      .SortableColumnFor(x => x.StartDate)
      .SortableColumnFor(x => x.EstimatedReleaseDate)
      .SortableColumnFor(x => x.ReleaseDate)
      .SortableColumnFor(x => x.ReleaseReason)
      .ColumnFor(x => x.TotalDays)
      .WithPaginatedFooter()
      )