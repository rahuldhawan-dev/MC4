@model MapCallMVC.Areas.HumanResources.Models.ViewModels.EditCovidIssue

@using (Form.BeginForm("Update", "CovidIssue", "HumanResources", new { Model.Id }))
{
    @Html.Partial("_FormFields", Model)

    @Html.ButtonGroup(@<text>
        @Control.SubmitButton("Save")
        @Html.LinkButton("Cancel", "Show", "CovidIssue", new { Model.Id })
    </text>)
}