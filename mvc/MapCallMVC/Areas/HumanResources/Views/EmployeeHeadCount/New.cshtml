@model MapCallMVC.Areas.HumanResources.Models.ViewModels.CreateEmployeeHeadCount

@using (Form.BeginForm("Create", "EmployeeHeadCount", "HumanResources"))
{
    @Html.Partial("_FormFields", Model)

    @Html.ButtonGroup(@<text>
        @Control.SubmitButton("Save")
        @Html.LinkButton("Cancel", "Search")
    </text>)
}