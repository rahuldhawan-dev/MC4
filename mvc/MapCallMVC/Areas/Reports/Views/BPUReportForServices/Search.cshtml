@model MapCallMVC.Areas.FieldOperations.Models.ViewModels.Services.Reports.SearchBPUReportForServices


@using (Form.BeginForm("Index", "BPUReportForServices"))
{
    <div class="field-column fc200">
        @Html.EditorFor(x => x.OperatingCenter)
        @Html.EditorFor(x => x.Year)
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Search").WithId("Search")
            @Control.ResetButton()
        </text>)
    </div>
}