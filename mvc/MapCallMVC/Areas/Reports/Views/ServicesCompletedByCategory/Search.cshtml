@model MapCallMVC.Areas.FieldOperations.Models.ViewModels.Services.Reports.SearchServicesCompletedByCategory

@using (Form.BeginForm("Index", "ServicesCompletedByCategory"))
{
    <div class="field-column fc200">
        @Html.EditorFor(x => x.OperatingCenter)
        @Html.EditorFor(x => x.DateInstalled)
        @Html.EditorFor(x => x.DeveloperServicesDriven)
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Search").WithId("Search")
            @Control.ResetButton()
        </text>)
    </div>
}