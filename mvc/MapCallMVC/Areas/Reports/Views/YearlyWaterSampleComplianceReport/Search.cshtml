@model SearchYearlyWaterSampleComplianceReport

@using (Form.BeginForm("Index", "YearlyWaterSampleComplianceReport")) {
    <div class="field-column fc200 search-fields">
	    @Html.EditorFor(x => x.State)
		@Html.EditorFor(x => x.OperatingCenter)
		@Html.EditorFor(x => x.EntityId)
		@Html.EditorFor(x => x.CertifiedYear)

        @Html.ButtonGroup(@<text>
                              @Control.SubmitButton("Search").WithId("Search")
                              @Control.ResetButton()
                           </text>)
    </div>
}