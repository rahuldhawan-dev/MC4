@model MapCallMVC.Areas.FieldOperations.Models.ViewModels.Services.Reports.SearchServicesRenewedSummary

@using (Form.BeginForm("Index", "ServicesRenewedSummary"))
{
    <div class="field-column fc200">
        @Html.EditorFor(x => x.OperatingCenter)
        @Html.EditorFor(x => x.Year)
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Search").WithId("Search")
            @Control.ResetButton()
        </text>)
    </div>
}