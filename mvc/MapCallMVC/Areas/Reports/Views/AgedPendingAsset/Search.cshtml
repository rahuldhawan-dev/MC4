@model MapCallMVC.Areas.FieldOperations.Models.ViewModels.SearchAgedPendingAsset

@using (Form.BeginForm("Index", "AgedPendingAsset"))
{
    <div class="field-column fc200 searchFields">
        @Html.EditorFor(x => x.OperatingCenter)
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Search").WithId("Search")
            @Control.ResetButton()
        </text>)
    </div>
}