@model MapCallMVC.Areas.Facilities.Models.ViewModels.SearchFacilityProcess

@(Html.TableFor(x => x)
      .TemplateColumnFor(x => Control.ActionLink("View", "Show", "FacilityProcess", new { id = x.Id }, null))
      .ColumnFor(x => x.Facility.OperatingCenter)
      .ColumnFor(x => x.Facility)
      .ColumnFor(x => x.Process.ProcessStage)
      .ColumnFor(x => x.Process)
      .WithPaginatedFooter())           
