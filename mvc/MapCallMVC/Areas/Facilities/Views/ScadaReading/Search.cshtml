@model MapCallMVC.Areas.Facilities.Models.SearchScadaReading

@using (Form.BeginForm("Index", "ScadaReading", "Facilities"))
{
    <div class="field-column fc100">
        @Html.EditorFor(x => x.TagName)
        @Html.EditorFor(x => x.UseRaw)
        @Html.EditorFor(x => x.StartDate)
        @Html.EditorFor(x => x.EndDate)

        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Search").WithId("Search")
            @Control.ResetButton()
        </text>)
    </div>
}