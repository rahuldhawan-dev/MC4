@model MapCallMVC.Areas.ProjectManagement.Models.ViewModels.EditEstimatingProjectMaterial

@using (Form.BeginForm("Update", "EstimatingProjectMaterial", "ProjectManagement", new {Model.Id}))
{
    <div class="field-column fc200">
        @Html.HiddenFor(m => m.EstimatingProject)
        @Html.EditorFor(x => x.AssetType)
        @Html.EditorFor(x => x.Material)    
        @Html.EditorFor(x => x.Quantity)
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Save")
            @Html.LinkButton("Cancel", "Show", "EstimatingProject", new { id = Model.EstimatingProject }, null)
        </text>)
    </div>
}