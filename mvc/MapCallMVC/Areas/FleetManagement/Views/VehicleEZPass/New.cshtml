@model MapCallMVC.Areas.FleetManagement.Models.VehicleEZPassViewModel

@using (Form.BeginForm("Create", "VehicleEZPass"))
{
    <div class="field-column fc200">
        @Html.Partial("_Form")
        @Html.ButtonGroup(@<text>
            @Control.SubmitButton("Save")
            @Html.LinkButton("Cancel", "Search")
        </text>)
    </div>
}
