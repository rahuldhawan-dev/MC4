@model MapCallMVC.Areas.Operations.Models.ViewModels.EditFamilyMedicalLeaveActCase

@using (Form.BeginForm("Update", "FamilyMedicalLeaveActCase", "Operations", new {Model.Id})) {
    <div class="field-column fc200">
        @Html.Partial("_FormFields", Model)
 
        @Html.ButtonGroup(@<text>
                               @Control.SubmitButton("Save")
                               @Html.LinkButton("Cancel", "Show", "FamilyMedicalLeaveActCase", new {Model.Id})
                           </text>)
    </div>
}