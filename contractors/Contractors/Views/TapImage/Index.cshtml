@using MMSINC.Results
@model Contractors.Models.ViewModels.SearchTapImage
@{
	ViewBag.Title = "Tap Images";
}
@(Html.TableFor(x => x, new {id = "search-results-table", @class = "results-table"})
        .WithCaption("Records found: " + Model.Count)
	    .TemplateColumnFor(x => Html.ActionLink("View", "Show", new { x.Id }))
	    .TemplateColumnFor(x => Html.ActionLink("PDF", "Show", new { x.Id, ext = ResponseFormatter.KnownExtensions.PDF }, new { target = "_blank" }))
        .SortableColumnFor(x => x.ServiceNumber)
        .SortableColumnFor(x => x.PremiseNumber)
        .SortableColumnFor(x => x.DateCompleted)
        .ColumnFor(x => x.Town.County.State)
        .ColumnFor(x => x.Town.County)
        .SortableColumnFor(x => x.Town)
        .SortableColumnFor(x => x.TownSection)
        .SortableColumnFor(x => x.StreetNumber)
        .SortableColumnFor(x => x.StreetPrefix)
        .SortableColumnFor(x => x.ApartmentNumber)
        .SortableColumnFor(x => x.Street)
        .SortableColumnFor(x => x.StreetSuffix)
        .SortableColumnFor(x => x.CrossStreet)
        .SortableColumnFor(x => x.ServiceType)
        .SortableColumnFor(x => x.Lot)
        .SortableColumnFor(x => x.Block)
        .SortableColumnFor(x => x.ServiceSize)
        .SortableColumnFor(x => x.ServiceMaterial)
        .SortableColumnFor(x => x.LengthOfService)
        .SortableColumnFor(x => x.IsDefaultImageForService)
        .SortableColumnFor(x => x.OfficeReviewRequired)
        .SortableColumnFor(x => x.CreatedAt)
        .WithPaginatedFooter())
