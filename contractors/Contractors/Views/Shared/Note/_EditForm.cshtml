@model Contractors.Models.ViewModels.EditNote

@using (var form = Form.BeginForm("Update", "Note", "", new { Model.Id }))
{
    form.HtmlAttributes["id"] = String.Format("updateNote{0}", Model.Id);
    @Control.TextAreaFor(n => n.Text).With(new {disabled = "disabled", rows = "2"})
}
