@{
    // ComboBox is in MMSINC.Core.Mvc but the script and css are not implemented on contractors.
    // That should probably be fixed.
    // This is here to stop an error from throwing.
    Html.SharedViewData()["CanDoComboBox"] = false;
}