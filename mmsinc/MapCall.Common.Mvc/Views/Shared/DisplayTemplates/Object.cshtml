@model object
@* It should be pointed out that this value is generated somewhere along the line by
   just calling ToString() on the model object. So the model will need to override that. 
   Otherwise it'll just display the Type name. *@
@RenderGenericDisplayForTemplate()